#include <stdio.h>

int utils_watch_config(); 
void listen_signal();

int main(int argn, char** argc) {
    printf("This is steven's codescope main entrance!\n");
    utils_watch_config();
    return 0;
}
