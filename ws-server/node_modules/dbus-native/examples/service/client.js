var dbus = require('../../index.js');
var bus = dbus.sessionBus();

var destination = 'test.singal.dest';
bus.invoke({ 
    path: '/test/signal/Object', 
    destination: destination, 
//    'interface': 'org.vasya.pupkin.reverser', 
    'interface': 'test.signal.Type', 
    member: 'Test', 
    signature: 'Test', body: 'does it really work?'
}, function(err, res) {
    console.log(err, res);
});

